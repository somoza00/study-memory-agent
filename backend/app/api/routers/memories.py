"""Router de memórias: endpoints de store e recall.

Por implementar:
- POST /api/memories      → delega a memory_service.store(text, metadata)
- GET  /api/memories/recall?query=&limit=&min_score= → delega a memory_service.recall(...)
"""
